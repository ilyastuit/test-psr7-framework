{% extends 'layout/default.html.twig' %}

{% block title %}{{ task.meta.title }}{% endblock %}

{% block meta %}
    <meta name="description" content="{{ task.meta.description }}" />
{% endblock %}

{% block breadcrumbs %}
    <ul class="breadcrumb">
        <li class="breadcrumb-item"><a href="{{ path('home') }}">Home</a></li>
        <li class="breadcrumb-item"><a href="{{ path('task') }}">Task</a></li>
        <li class="breadcrumb-item active">{{ task.title }}</li>
    </ul>
{% endblock %}

{% block content %}
    <h1>{{ task.title }}</h1>

    <div class="card mb-3">
        <div class="card-header">
            {{ task.date|date('Y-m-d') }}
        </div>
        <div class="card-body">{{ task.content|nl2br }}</div>
    </div>

    <h2>Comments</h2>

    {% for comment in task.comments %}
        <div class="card mb-3">
            <div class="card-header">
                <span class="float-right">{{ comment.date|date('Y-m-d') }}</span>
                {{ comment.author }}
            </div>
            <div class="card-body">{{ comment.text|nl2br }}</div>
        </div>
    {% endfor %}
{% endblock %}